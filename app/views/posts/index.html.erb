<h1>投稿一覧</h1>

<% @posts.each do |post| %>
  <div>
    <h2><%= post.title %></h2>
    <p><%= post.content %></p>
    <p>カテゴリー: <%= category_label(post.category) %></p>
    <p>野菜: <%= post.vegetable.name if post.vegetable %></p>
    <hr>
  </div>
<% end %>