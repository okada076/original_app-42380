<div class="growing-guide">
  <h2 class="guide-title"><%= @vegetable.name %>の育て方</h2>

  <% if @steps.any? %>
  <ol class="step-list">
    <% @steps.each do |step| %>
      <% progress = @progresses[step.id] %>
      <li class="step-item">
        <label class="step-label">
          <%= check_box_tag "step_checkbox_#{step.id}", '1', progress&.checked, class: "step-checkbox", id: "step_checkbox_#{step.id}", onchange: "this.form.submit();" %>
          <div class="step-card">
            <strong>STEP <%= step.step_number %>: <%= step.title %></strong><br>
            <p><%= step.content %></p>
          </div>
        </label>
      </li>
    <% end %>
  </ol>
<% else %>
  <p>この野菜の育て方手順はまだ登録されていません。</p>
<% end %>
