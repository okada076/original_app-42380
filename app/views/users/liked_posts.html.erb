<h2><%= @user.nickname %> さんがいいねした投稿</h2>

<% if @liked_posts.any? %>
  <div class="post-list">
    <% @liked_posts.each do |post| %>
      <div class="post-card">
        <% if post.image.attached? %>
          <%= image_tag url_for(post.image), class: "post-image" %>
        <% end %>

        <div class="card-body">
          <h5 class="post-title"><%= link_to post.title, post_path(post) %></h5>
          <p class="post-meta">
            投稿者：<%= post.user.nickname %>｜野菜：<%= post.vegetable.name %>
          </p>

          <% if post.grow_log? %>
            <span class="category-badge grow_log">育成記録</span>
          <% elsif post.trouble_note? %>
            <span class="category-badge trouble_note">つまづきノート</span>
          <% end %>

          <%= link_to '詳細を見る', post_path(post), class: "btn btn-outline-success btn-sm mt-2 w-100" %>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <p>まだいいねした投稿はありません。</p>
<% end %>
